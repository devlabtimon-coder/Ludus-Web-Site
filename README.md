
  # Ludus Dashboard Structure

  This is a code bundle for Ludus Dashboard Structure. The original project is available at https://www.figma.com/design/zypVtvfYxAZuNn3VVbRR5f/Ludus-Dashboard-Structure.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  